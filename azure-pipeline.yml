resources:
  - repo: self

pool:
  name: $(Parameters.PoolName)
  vmImage: "$(Parameters.vmImageName)"

variables:
  test: "5>3 ? true : false"

steps:
  - script: echo running $(test)
    displayName: "test"
