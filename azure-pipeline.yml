variables:
  configureMultiRegion: $(check)

stages:
- stage: 'Prepare'
  displayName: 'Prepare'
  
  jobs:
  - job: 'Setup'
    condition: $(check)
    pool:
      name: Hosted VS2017
    variables:
      configureMultiRegiona: (variables.configureMultiRegion, true)
    steps:
    - task: DotNetCoreInstaller@0
      condition: $
      displayName: 'Install'
      inputs:
        packageType: 'sdk'
        version: '2.2.105'
  
