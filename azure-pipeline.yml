resources:
- repo: self

pool:
  name: Hosted VS2017
  demands: azureps
  
steps:

- powershell: 'Write-Host 0'
  condition: eq('$(Parameters.configureMultiRegion)', true)
  displayName: 'PowerShell Script'
  enabled: true
