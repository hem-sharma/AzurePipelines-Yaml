resources:
- repo: self

pool:
  name: Hosted VS2017
  demands: azureps
  
steps:

- powershell: 'Write-Host "$(backends)"'
  condition: eq($(Parameters.configureMultiRegion), true)
  displayName: 'PowerShell Script'
  enabled: true
