resources:
- repo: self

variables:
  skip: false

pool:
  name: Hosted VS2017
  demands: azureps
  
steps:

- powershell: 'Write-Host 0'
  condition: eq(variables.skip, true)
  displayName: 'PowerShell Script'
  enabled: true
